import { Container } from '@/components/Container';

export const Header = () => {
  return (
    <header className="header relative z-30">
      <Container>
        <p>It is header</p>
      </Container>
    </header>
  );
};
