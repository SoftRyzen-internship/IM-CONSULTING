import { Container } from '@/components/Container';

export const Header = () => {
  return (
    <header className="header">
      <Container>
        <p>It is header</p>
      </Container>
    </header>
  );
};
