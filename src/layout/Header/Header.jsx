import { Container } from '@/components/Container';

export const Header = () => {
  return (
    <header>
      <Container className="header">
        <p>It is header</p>
      </Container>
    </header>
  );
};
